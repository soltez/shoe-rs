[package]
name = "shoe-rs"
version = "0.1.0"
edition = "2024"
description = "Cactus Kev's playing card dealing shoe library for Rust"
license = "LGPL-3.0"

[dependencies]

[dev-dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
